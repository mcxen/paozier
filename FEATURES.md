# 狍子 (Paozier) 功能特性详解

本文档详细介绍狍子的各项核心功能和使用方法。

---

## 1. 三引擎融合搜索

狍子集成了三种强大的搜索引擎，为不同的搜索场景提供最优解决方案：

### SearchKit 引擎
- 基于 Apple 原生 SearchKit 框架
- 采用 TF-IDF 算法进行相关性排序
- 适合英文和长文本相关性搜索

### SQLite FTS5 引擎
- 支持 CJK 分词和全文索引
- 快速精确的关键词匹配
- 适合中文和结构化文本搜索

### Tantivy 引擎（Rust）
- N-gram 分词技术
- 增强的中文子串匹配能力
- 高性能并发搜索



---

## 2. 全局快捷搜索

### 全局搜索弹窗（⌘⇧F）

在任意应用中按下 `⌘⇧F`，即可呼出 Spotlight 风格的搜索窗口：

- 悬浮于所有窗口之上
- 输入即搜，实时显示结果
- 支持键盘快速导航
- 按 `ESC` 关闭，按 `Enter` 打开选中文件

**截图位置：全局搜索弹窗**

![全局搜索弹窗](./screenshots/02-global-search.png)

### 快捷搜索面板（⌘⇧K）

按下 `⌘⇧K` 打开快速搜索面板：

- 轻量级浮动面板
- 快速查询和跳转
- 适合频繁搜索场景

**截图位置：快捷搜索面板**

![快捷搜索面板](./screenshots/03-quick-panel.png)

---

## 3. 即时文件搜索（Grep 模式）

无需等待索引完成，使用 ripgrep/grep 直接搜索文件内容：

- 实时搜索，零延迟
- 支持正则表达式
- 适合临时查找和代码搜索
- 自动高亮匹配行

### 使用方法
在搜索栏中选择 "Grep 搜索" 模式，输入关键词或正则表达式即可。

**截图位置：Grep 搜索模式**

![Grep 搜索](./screenshots/04-grep-search.png)

---

## 4. 高级查询语法

狍子支持丰富的查询语法，满足各种复杂搜索需求：

### 布尔运算符
- `关键词1 AND 关键词2` — 同时包含两个词
- `关键词1 OR 关键词2` — 包含任意一个词
- `NOT 关键词` 或 `-关键词` — 排除某个词

### 短语匹配
- `"完整短语"` — 精确匹配整个短语

### 通配符
- `关键*` — 前缀通配
- `*关键*` — 任意位置匹配

### 邻近词搜索
- `关键词1 NEAR/5 关键词2` — 两词在 5 个词距离内

### 模糊空格匹配
- `关键词` — 自动匹配 "关键-词"、"关键_词" 等变体

**截图位置：高级查询语法示例**

![高级查询](./screenshots/05-advanced-query.png)

---

## 5. 搜索结果高亮与预览

### 匹配片段高亮

搜索结果列表显示：
- 文件名和路径
- 匹配关键词的上下文片段
- 高亮显示匹配词
- 相关性评分

**截图位置：搜索结果列表**

![搜索结果列表](./screenshots/06-results-list.png)

### Live Preview 实时预览

点击搜索结果，右侧面板实时显示：
- 完整文件内容
- 自动滚动到匹配位置
- 高亮所有匹配词
- 支持多格式预览

**截图位置：实时预览面板**

![实时预览](./screenshots/07-live-preview.png)

---

## 6. 多格式文件预览

狍子支持多种文件格式的原生预览：

### PDF 预览
- 基于 PDFKit 框架
- 支持页面跳转和缩放
- 高亮 PDF 内匹配文本

**截图位置：PDF 预览**

![PDF 预览](./screenshots/08-pdf-preview.png)

### Office 文档预览
- Word (docx/doc)
- Excel (xlsx/xls)
- PowerPoint (pptx/ppt)
- 使用 QuickLook 技术预览

**截图位置：Office 文档预览**

![Office 预览](./screenshots/09-office-preview.png)

### 代码文件预览
- 支持语法高亮
- 显示行号
- 支持多种编程语言

**截图位置：代码文件预览**

![代码预览](./screenshots/10-code-preview.png)

---

## 7. 图片 OCR 文字识别

使用 Apple Vision 框架对图片进行文字识别：

- 自动提取图片中的文字
- 支持中英文 OCR
- 识别结果加入索引，可被搜索
- 支持常见图片格式（PNG、JPG、HEIC 等）

### 使用方法
添加包含图片的文件夹到索引，狍子会自动对图片进行 OCR 处理。

**截图位置：OCR 识别结果**

![OCR 文字识别](./screenshots/11-ocr-results.png)

---

## 8. 文件夹管理与增量索引

### 添加文件夹

在侧边栏点击 "+" 按钮添加要索引的文件夹：
- 支持添加多个文件夹
- 自动监控文件变化
- 增量更新索引

**截图位置：文件夹管理界面**

![文件夹管理](./screenshots/12-folder-management.png)

### 索引进度显示

索引时显示实时进度：
- 当前正在处理的文件
- 已索引文件数量
- 索引速度
- 预计剩余时间

**截图位置：索引进度**

![索引进度](./screenshots/13-indexing-progress.png)

### 增量索引

文件变化时自动增量更新：
- 监控文件系统事件
- 仅更新变化的文件
- 删除已移除文件的索引
- 高效节省资源

---

## 9. 搜索历史与书签

### 搜索历史

自动记录搜索历史：
- 查看最近搜索
- 快速重复搜索
- 清除历史记录

**截图位置：搜索历史**

![搜索历史](./screenshots/14-search-history.png)

### 书签功能

为重要文件添加书签：
- 快速访问常用文件
- 分类管理书签
- 添加备注说明

**截图位置：书签管理**

![书签管理](./screenshots/15-bookmarks.png)

---

## 10. HTTP 搜索服务

狍子内置 HTTP 服务器（端口 9880），提供 Web API 和网页搜索界面：

### Web 搜索界面

在浏览器中访问 `http://localhost:9880`：
- 响应式网页设计
- 与桌面应用相同的搜索能力
- 适合远程访问和分享

**截图位置：Web 搜索界面**

![Web 界面](./screenshots/16-web-interface.png)

### REST API

提供 RESTful API 接口：

```bash
# 搜索
curl "http://localhost:9880/api/search?q=关键词"

# 查询状态
curl "http://localhost:9880/api/status"

# 获取文件内容
curl "http://localhost:9880/api/file?path=/path/to/file"
```

**截图位置：API 响应示例**

![API 示例](./screenshots/17-api-response.png)

---

## 11. MCP 服务器集成

内置 MCP (Model Context Protocol) 服务器（端口 9881），供 AI 工具集成：

### 支持的 AI 工具
- Cursor
- Claude Desktop
- 其他支持 MCP 协议的 AI 工具

### 可用工具

MCP 服务器提供以下工具供 AI 调用：

| 工具名称 | 功能描述 |
|---------|---------|
| `search_documents` | 全文搜索文档 |
| `get_document_content` | 获取文档完整内容 |
| `search_memos` | 搜索 Memos 笔记 |
| `grep_search` | 正则表达式搜索 |
| `index_folder` | 索引指定文件夹 |
| `list_indexed_folders` | 列出已索引文件夹 |
| `index_status` | 查询索引状态 |
| `list_files` | 列出文件 |
| `get_file_info` | 获取文件信息 |
| `remove_folder` | 移除索引文件夹 |
| `reindex_all` | 重建全部索引 |

### 配置示例

在 Cursor 或 Claude Desktop 的配置文件中添加：

```json
{
  "mcpServers": {
    "paozier": {
      "url": "http://localhost:9881"
    }
  }
}
```

**截图位置：MCP 配置与使用**

![MCP 集成](./screenshots/18-mcp-integration.png)

---

## 12. Memos 笔记源集成

狍子可以连接外部 Memos 笔记服务，统一搜索本地文件和云端笔记：

### 配置 Memos 源

在设置中添加 Memos 服务器：
- 输入 Memos 服务器地址
- 配置访问令牌
- 自动同步笔记内容

### 搜索 Memos 笔记

搜索时可选择包含 Memos 笔记：
- 统一搜索结果
- 显示笔记来源标识
- 点击打开笔记详情

**截图位置：Memos 集成**

![Memos 集成](./screenshots/19-memos-integration.png)

---

## 13. TUI 终端客户端

基于 Textual 框架的全功能终端用户界面：

### 特性
- 完整的搜索功能
- 终端内文件预览
- 键盘快捷操作
- 适合 SSH 远程使用

### 启动方法

```bash
cd Sources/TUI
./run_tui.command
```

首次运行会自动创建虚拟环境并安装依赖。

**截图位置：TUI 界面**

![TUI 客户端](./screenshots/20-tui-client.png)

---

## 14. 正则表达式搜索

支持强大的正则表达式搜索：

### 使用场景
- 复杂模式匹配
- 代码搜索
- 数据提取

### 示例

- `\b\d{3}-\d{4}\b` — 匹配电话号码
- `function\s+\w+\(` — 匹配函数定义
- `https?://[\w./-]+` — 匹配 URL

**截图位置：正则表达式搜索**

![正则搜索](./screenshots/21-regex-search.png)

---

## 15. 报告收集功能

收集和整理搜索结果，生成专题报告：

### 使用方法
1. 执行搜索
2. 选择相关结果
3. 添加到报告
4. 导出或分享

### 应用场景
- 研究项目资料收集
- 文献综述整理
- 知识库构建

**截图位置：报告收集**

![报告收集](./screenshots/22-compendium.png)

---

## 16. 自定义应用图标

支持自定义应用图标，个性化你的狍子：

### 更改图标
1. 在设置中选择"外观"
2. 点击"更换图标"
3. 选择自定义图片
4. 应用更改

**截图位置：自定义图标**

![自定义图标](./screenshots/23-custom-icon.png)

---

## 17. 性能监控与统计

查看索引和搜索的性能统计：

### 统计信息
- 已索引文件总数
- 索引占用空间
- 搜索响应时间
- 缓存命中率

**截图位置：性能统计**

![性能统计](./screenshots/24-performance-stats.png)

---

## 18. 深色模式支持

完整支持 macOS 深色模式：
- 自动跟随系统主题
- 护眼的暗色配色
- 搜索结果高亮清晰可见

**截图位置：深色模式**

![深色模式](./screenshots/25-dark-mode.png)

---

## 使用技巧

### 提高搜索效率
1. **使用布尔运算符**：精确筛选结果
2. **善用通配符**：找到词形变化
3. **组合多个引擎**：对比不同排序结果
4. **添加书签**：快速访问常用文件

### 优化索引性能
1. **选择性索引**：只添加需要搜索的文件夹
2. **排除大文件**：在设置中配置文件大小限制
3. **定期清理**：删除不再需要的索引

### 高级用法
1. **结合 MCP 使用**：让 AI 助手帮你搜索和整理
2. **使用 HTTP API**：集成到自己的工具链
3. **TUI 远程访问**：SSH 连接后使用终端客户端

---

## 常见问题

**Q: 索引需要多长时间？**  
A: 取决于文件数量和大小。一般 1GB 文档约需 1-2 分钟。

**Q: 支持哪些文件格式？**  
A: 支持 PDF、Office 文档、文本、代码、Markdown、EPUB 等 30+ 种格式。

**Q: 如何卸载？**  
A: 删除应用并清理 `~/Library/Application Support/Paozier/` 目录。

**Q: 索引会占用多少空间？**  
A: 通常为原文件大小的 10-30%，取决于文件类型。

**Q: 是否支持网络驱动器？**  
A: 支持，但网络延迟可能影响索引速度。

---

## 总结

狍子是一款功能强大的本地全文搜索工具，适合：

- 📚 知识工作者整理和检索大量文档
- 💻 开发者快速搜索代码和技术文档
- 📝 研究人员管理论文和研究资料
- 🎓 学生整理学习笔记和课程材料

通过三引擎融合、全局快捷键、丰富的预览功能和 AI 工具集成，狍子让你在文件的森林中，像狍子一样灵敏地找到任何信息。

---

*本文档持续更新中，更多功能特性敬请期待。*
